# ================================================================
# Smart Campus Operations Hub - Application Configuration
# ================================================================

# ---------- Server ----------
server.port=9090
spring.application.name=smart-campus-hub

# ---------- MongoDB Atlas ----------
spring.data.mongodb.uri=mongodb+srv://Inushka:Inushka2000@cluster0.d6fxrlw.mongodb.net/smart-campus?retryWrites=true&w=majority&appName=Cluster0

# ---------- JWT ----------
# TODO: Member 1 - Replace this secret with a strong 256-bit key before production
app.jwt.secret=SmartCampusHubSuperSecretKeyForJWTTokenSigning2024
app.jwt.expiration-ms=86400000

# ---------- CORS ----------
# TODO: Member 1 - Update allowed origins for deployed frontend URL
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# ---------- Logging ----------
logging.level.com.smartcampus=DEBUG
logging.level.org.springframework.security=DEBUG


# ---------- Multipart File Upload ----------
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=15MB

# ---------- Google OAuth2 ----------
spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET
spring.security.oauth2.client.registration.google.scope=openid,email,profile
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
app.oauth.frontend-redirect-url=http://localhost:5173/oauth/callback
